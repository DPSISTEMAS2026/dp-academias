/**
 * El esquema demo vive en /supabase/schema.sql
 * Este archivo YA NO conecta a ningún proyecto de Supabase
 * No conectar a qbimxygcjjmosifsqbko (Ranas producción)
 * ni a xtcxbxvbtxnmuaylrhmr (backup).
 */
console.log(`
🥋 DP Sistemas — no uses este script contra producción.

1. Crea un proyecto NUEVO en https://supabase.com
2. Copia .env.example a .env y pega SUPABASE_URL + SUPABASE_ANON_KEY
3. En SQL Editor pega y ejecuta:
   - supabase/schema.sql
   - supabase/seed.sql
`);
process.exit(0);
